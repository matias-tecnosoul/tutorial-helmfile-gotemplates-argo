---
# Overrides para producción

baseDomain: example.com
clusterIssuer: letsencrypt-prod

postgres:
  persistence:
    enabled: true
    size: 20Gi
    className: gp3
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 2000m
      memory: 4Gi
