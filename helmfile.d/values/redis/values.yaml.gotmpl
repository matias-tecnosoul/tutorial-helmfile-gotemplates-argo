# helmfile.d/values/redis/values.yaml.gotmpl (MÍNIMO)
---
image:
  repository: redis
  tag: "7-alpine"

# Password mediante userPassword
userPassword: {{ .Values.redis.password | quote }}

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi