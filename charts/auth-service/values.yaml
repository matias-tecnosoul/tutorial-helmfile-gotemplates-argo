# ... (deployment, service config)

ingress:
  enabled: false
  className: nginx
  annotations: {}
  hosts:
    - host: auth.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []